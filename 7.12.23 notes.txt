Order of instructions does make a difference. GPT reads forward and is meant to predict the next word so put instructions first then examples.
You can use things like between <Begin> and <end> will be [example/data/anything you want here]
use words such as must instead of should. basically don't be nice to LLMS, be demanding